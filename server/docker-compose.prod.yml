version: "3.9"
services:
  api:
    build:
      context: .
      target: production
    ports:
      - 4000:5000
  db:
    container_name: solar-ts-postgres
    image: "postgres"
    env_file:
      - .env
    ports:
      - 4444:5432
